apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: apc-api
  namespace: apc-dev
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: apc-api
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
